name: CI Minimal
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
  codeql:
    uses: antomicblitz/opencode-config/.github/workflows/codeql.yml@v1
